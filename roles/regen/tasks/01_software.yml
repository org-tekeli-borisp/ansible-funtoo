---
- name: regen - ego sync
  shell: |
    /usr/bin/ego sync
  register: out
- debug: var=out.stdout_lines

- name: regen - ensure dev-python/jinja is installed
  portage:
    package: dev-python/jinja
    state: present
    quiet: true
  register: out
- debug: var=out.stdout_lines

- name: regen - ensure dev-python/pyzmq  is installed
  portage:
    package: dev-python/pyzmq
    state: present
    quiet: true
  register: out
- debug: var=out.stdout_lines

- name: regen - ensure dev-python/pip  is installed
  portage:
    package: dev-python/pip
    state: present
    quiet: true
  register: out
- debug: var=out.stdout_lines

- name: regen - ensure dev-python/lxml  is installed
  portage:
    package: dev-python/lxml
    state: present
    quiet: true
  register: out
- debug: var=out.stdout_lines

